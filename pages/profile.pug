<!DOCTYPE html>
html(lang="en")
  include includes/head.pug
  body
    include includes/header.pug
    h1 This is your home page
  
  body
    div
      h2 #{user.name}
      p age: #{user.age}
      p job: #{user.job}
      p best friend is #[a(href=`/profiles/${user.bestFriend.toLowerCase()}`)= user.bestFriend]
    include includes/footer.pug